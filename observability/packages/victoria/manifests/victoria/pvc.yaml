apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: victoria
spec:
  storageClassName: ${STORAGE_CLASS_NAME:=null}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi  
      